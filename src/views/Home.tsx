import * as React from 'react';
import { useAppState } from '../hooks/useAppState'

const Home = () => {
  const appState = useAppState();

  return (
    <div>
    </div>
  )
};

export default Home;